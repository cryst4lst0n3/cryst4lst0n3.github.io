---
title: "Write Up for cryptpad"
date: 2026-03-20 10:00:00 +0700
categories: [CrackMe]
tags: [crackme-ctf-2026]
---

Đề : [Link Chall](https://github.com/crackmesone/ctf-2026-challenges-public/blob/main/cryptpad/handout/cryptpad.exe)

# Phân tích Hàm Check_key
```c
char __stdcall sub_4014EB(_BYTE *pwd, int len, int logic)
{
  unsigned int v3; // ecx
  _BYTE *v4; // esi
  int v5; // eax
  char result; // al
  _BYTE *v7; // esi
  _BYTE *v8; // edi
  DWORD v9; // ecx
  int v10; // edx
  int v11; // ecx
  _BYTE *v12; // esi
  int v13; // ecx
  int v14; // ebx
  int v15; // ebx
  _BYTE *v16; // esi
  int v17; // eax
  int v18; // ecx
  char v19; // dl
  _BYTE *v20; // edx
  int v21; // eax
  int v22; // ebx
  int v23; // ecx
  int v24; // ecx
  char v25; // dh
  char v26; // dl
  _BYTE *v27; // esi
  _BYTE *v28; // edi
  DWORD v29; // ecx
  int v30; // edx
  _BYTE *v31; // edi
  _BYTE *v32; // [esp-8h] [ebp-Ch]
  int v33; // [esp-4h] [ebp-8h]

  if ( logic )
  {
    if ( logic != 1 )
      return MessageBoxA(0, 0, 0, 0);
  }
  else
  {
    v3 = *(_DWORD *)&pwd[len - 1];
    v4 = &pwd[len - 1 - v3];
    qmemcpy(offset, v4, v3);
    v5 = *((_DWORD *)v4 - 1);
    *((_DWORD *)v4 - 1) = 0;
    len = v5;
  }
  v7 = pwd;
  v8 = offset;
  v9 = NumberOfBytesWritten;
LABEL_6:
  v10 = 0;
  do
  {
    *v7++ ^= *v8++;
    if ( ++v10 == 8 )
      goto LABEL_6;
    --v9;
  }
  while ( v9 );
  v11 = 256;
  do
  {
    secret[(unsigned __int8)-(char)v11] = -(char)v11;
    --v11;
  }
  while ( v11 );
  v12 = &key;
  v13 = 256;
  v14 = 0;
  do
  {
    if ( v14 >= 8 )
      v14 = 0;
    *v12++ = offset[v14++];
    --v13;
  }
  while ( v13 );
  v15 = 0;
  v16 = v12 - 256;
  v17 = 0;
  v18 = 256;
  do
  {
    LOBYTE(v15) = secret[v17] + v16[v17] + v15;
    v19 = secret[v17];
    secret[v17] = secret[v15];
    secret[v15] = v19;
    ++v17;
    --v18;
  }
  while ( v18 );
  v20 = pwd;
  v21 = 0;
  v22 = 0;
  v23 = len;
  do
  {
    v33 = v23;
    v24 = (unsigned __int8)(v21 + 1);
    v32 = v20;
    v25 = secret[v24];
    LOBYTE(v22) = v25 + v22;
    v26 = secret[v22];
    secret[v24] = v26;
    secret[v22] = v25;
    LOBYTE(v24) = secret[(unsigned __int8)(v25 + v26)] ^ pwd[v21];
    v20 = v32;
    v32[v21++] = v24;
    v23 = v33 - 1;
  }
  while ( v33 != 1 );
  v27 = pwd;
  v28 = offset;
  v29 = NumberOfBytesWritten;
LABEL_20:
  v30 = 0;
  do
  {
    result = *v28 ^ *v27;
    *v27++ = result;
    ++v28;
    if ( ++v30 == 8 )
      goto LABEL_20;
    --v29;
  }
  while ( v29 );
  if ( logic == 1 )
  {
    v31 = &pwd[len - 13 + RandomBufferLength];
    *(_DWORD *)v31 = len;
    v31 += 4;
    qmemcpy(v31, offset, 8u);
    v31[8] = 8;
    return RandomBufferLength + len;
  }
  return result;
}
```

# Nhận xét

-Phỏng đoán : Tác giả sử dụng kỹ thuật làm rối hoặc stripped file làm cho hàm check_key khó đọc ở vòng lặp, các biến thừa rất nhiều

- Ta phải tiến hành dịch 1-1 sang mã python từ đó rút gọn để ra được phương pháp mã hóa

# Dịch ngược sang mã python và làm gọn lại :

```python
def memcpy(dest, src, n,m ,j):
    for i in range(0,j):
        dest[i+n] = src[m+i]
    return dest

def check_key(pwd, NumberOfBytesWritten,  RandomBufferLength):

    len = NumberOfBytesWritten + RandomBufferLength + 1
    offset = [0x08, 0x62, 0x70, 0xF1, 0x27, 0x73, 0xA7, 0xBE]
    v3 = (pwd[len-1] |
          pwd[len] << 8 |
          pwd[len+1] << 16 |
          pwd[len+2] << 24) & 0xffffffff
    v4 = len - 1 - v3
    offset = memcpy(offset, pwd, 0, v4,v3)
    v5 = (pwd[v4-4] |
          pwd[v4-3] << 8 |
          pwd[v4-2] << 16 |
          pwd[v4-1] << 24) & 0xffffffff
    pwd[v4-4] = 0
    pwd[v4-3] = 0
    pwd[v4-2] = 0
    pwd[v4-1] = 0
    len = v5   
    
    for i in range(NumberOfBytesWritten):
        pwd[i] ^= offset[i%8]
    secret = list(range(256))
    key = [0] * 256 
    for i in range(256):
        key[i] = offset[i%8] 
    p = 0
    for i in range(256):     
        p = (secret[i]+key[i]+p) % 256
        secret[i], secret[p] = secret[p], secret[i]
    j = 0
    for i in range(len):
        j = (secret[(i+1) % 256]+j) % 256
        secret[(i+1) % 256] ,secret[j] = secret[j], secret[(i+1)%256]
        pwd[i] = (secret[(secret[(i+1) % 256]+secret[j]) % 256] ^ pwd[i]) % 256
    result = 0
    for i in range(NumberOfBytesWritten):
        result = pwd[i] ^ offset[i%8]
        pwd[i] = result  
    for i in range(4):
        pwd[len-13+RandomBufferLength+i] = (len >> 8*(i)) & 0xff
    pwd = memcpy(pwd,offset,len-9+RandomBufferLength,0,8)
    pwd[len-9+RandomBufferLength + 8] = 8
    return RandomBufferLength + len
  ```
## Nhận Xét

- Cấu trúc ban đầu : __BytesWritten__  | __RandomBuffer___ | ký tự kết thúc <phỏng đoán __RandomBuffer__ và __BytesWritten__ được phân chia rõ ràng>

- Cấu trúc sau khi được mã hóa : __BytesWritten__ | __RandomBuffer__ | __NumberBytesWritten__ | __Offset__ | ký tự có giá trị 8

- Key cố định và Secret có bị biến đổi

- Thuật toán  khó chịu xuất hiện ở vòng for thứ 3 của hàm __check_key__ làm biến dạng secret bằng phương trình sau __p' = secret[p'] + key[i] + p mod 256 , nó khó ở chỗ đây là phương trình thặng dư 2 biến nghĩa là có thể nó có hơn 1 nghiệm cặp (p,p')



